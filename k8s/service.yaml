apiVersion: v1
kind: Service
metadata:
  name: cell-dino-service
  namespace: cell-dino-inference
spec:
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
  selector:
    app: cell-dino